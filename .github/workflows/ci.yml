name: CI - Testes Unificados Agibank

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  tests:
    name: "🧪 Execução de Testes Full Stack"
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'maven'

      - name: Instalar Chrome (para o módulo Web)
        run: |
          sudo apt-get update
          sudo apt-get install -y google-chrome-stable

      - name: Executar todos os módulos
        run: mvn clean test
        continue-on-error: true

      # PASSO CORRIGIDO: Unificação Robusta
      - name: Unificar Resultados Allure
        if: always()
        run: |
          mkdir -p all-results
          # Copia os resultados se as pastas existirem, sem quebrar o build
          [ -d "teste-blogdoagi/target/allure-results" ] && cp -r teste-blogdoagi/target/allure-results/* all-results/ || echo "Sem resultados Web"
          [ -d "teste-dogapi/target/allure-results" ] && cp -r teste-dogapi/target/allure-results/* all-results/ || echo "Sem resultados API"

      - name: Carregar histórico do Allure (gh-pages)
        uses: actions/checkout@v4
        if: always()
        continue-on-error: true
        with:
          ref: gh-pages
          path: gh-pages-dir

      - name: Allure Report Action
        uses: simple-elf/allure-report-action@master
        if: always()
        with:
          allure_results: "all-results"
          gh_pages: gh-pages-dir
          allure_report: allure-report
          allure_history: allure-history
          keep_reports: 20

      - name: Deploy do Relatório para o GitHub Pages
        if: always()
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_branch: gh-pages
          publish_dir: allure-history